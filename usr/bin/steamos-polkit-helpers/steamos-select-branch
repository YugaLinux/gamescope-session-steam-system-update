#!/usr/bin/bash

set -eu

if [ ! -x /usr/bin/steamos-select-branch ]; then
    # "rel" is the stable branch on SteamOS
    echo "rel"
    exit 0
fi

if [[ $EUID -ne 0 ]];
then
    exec pkexec --disable-internal-agent "$0" "$@"
fi

exec /usr/bin/steamos-select-branch "$@"

